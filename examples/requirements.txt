# Example-only dependencies. The plugin itself (livekit-plugins-munsit) and
# its base dep (livekit-agents) are installed by `uv sync` from the repo root.
livekit-plugins-silero
livekit-plugins-openai
livekit-plugins-cartesia
python-dotenv>=1.0.1
