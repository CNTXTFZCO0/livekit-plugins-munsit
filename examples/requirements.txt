# Dependencies for running the examples in this directory.
#
# Pulls livekit-plugins-munsit from PyPI; pin to a known-good version. The
# plugin already declares livekit-agents as a transitive dep, so installing
# this list alone is enough to run any example after `cp .env.example .env`.
livekit-plugins-munsit>=0.3.0
livekit-plugins-silero
livekit-plugins-openai
livekit-plugins-cartesia
python-dotenv>=1.0.1
